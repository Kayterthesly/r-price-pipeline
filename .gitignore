.Rproj.user
.Rhistory
.RData
.Ruserdata
# Secrets — NEVER commit
.Renviron

# Generated data and models
data/
models/*.rds
models/*.json

# renv library (huge, not committed — lockfile is committed instead)
renv/library/
renv/staging/
renv/sandbox/

# R history
.Rhistory
.RData